const attributes = {
    selectControl: {
        type: 'string',
        default: 32,
    },
    heading: {
        type: 'string',
    },
    subheading: {
        type: 'string',
    },
};

export default attributes;
